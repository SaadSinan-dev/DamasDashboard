include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  errors:
    # Anything the analyzer can prove is broken should fail the build, not sit
    # in the output as an informational note.
    invalid_annotation_target: ignore
    missing_required_param: error
    missing_return: error
    dead_code: error
    unused_import: error
    unused_local_variable: error
    unused_element: error
    # Async and lifecycle mistakes are the main source of runtime crashes in
    # Flutter apps, so they are promoted from lint to error.
    use_build_context_synchronously: error
    unawaited_futures: error
    avoid_print: error

  exclude:
    # Generated by `flutter gen-l10n`; not hand-maintained.
    - lib/core/l10n/generated/**
    - "**/*.g.dart"
    - "**/*.freezed.dart"

linter:
  rules:
    # ── Correctness ──────────────────────────────────────────────────────
    always_declare_return_types: true
    avoid_dynamic_calls: true
    avoid_empty_else: true
    avoid_slow_async_io: true
    avoid_type_to_string: true
    cancel_subscriptions: true
    close_sinks: true
    comment_references: true
    literal_only_boolean_expressions: true
    no_adjacent_strings_in_list: true
    test_types_in_equals: true
    throw_in_finally: true
    unnecessary_statements: true

    # ── Immutability ─────────────────────────────────────────────────────
    prefer_const_constructors: true
    prefer_const_constructors_in_immutables: true
    prefer_const_declarations: true
    prefer_const_literals_to_create_immutables: true
    prefer_final_fields: true
    prefer_final_in_for_each: true
    prefer_final_locals: true

    # ── Style and readability ────────────────────────────────────────────
    # `always_put_required_named_parameters_first` and
    # `prefer_expression_function_bodies` are deliberately left off: the first
    # fights the Flutter convention of leading with `super.key`, and the second
    # would turn every `build` method into an arrow function wrapping a deep
    # widget tree, which is harder to read and noisier to diff.
    avoid_bool_literals_in_conditional_expressions: true
    avoid_redundant_argument_values: true
    avoid_returning_null_for_void: true
    avoid_unused_constructor_parameters: true
    avoid_void_async: true
    cascade_invocations: true
    directives_ordering: true
    join_return_with_assignment: true
    noop_primitive_operations: true
    omit_local_variable_types: false
    prefer_asserts_in_initializer_lists: true
    prefer_if_elements_to_conditional_expressions: true
    prefer_null_aware_method_calls: true
    prefer_single_quotes: true
    # `require_trailing_commas` is left off: it disagrees with `dart format`
    # wherever the formatter wraps a single argument onto its own line, and
    # `dart format` is the authority on layout in this project.
    sized_box_shrink_expand: true
    sort_constructors_first: true
    type_annotate_public_apis: true
    unnecessary_await_in_return: true
    unnecessary_lambdas: true
    unnecessary_parenthesis: true
    unnecessary_raw_strings: true
    use_named_constants: true
    use_string_buffers: true

    # ── Flutter specifics ────────────────────────────────────────────────
    avoid_unnecessary_containers: true
    sized_box_for_whitespace: true
    use_colored_box: true
    use_decorated_box: true
    # Directional insets and alignments so Arabic (RTL) mirrors correctly.
    use_full_hex_values_for_flutter_colors: true

    # ── Documentation ────────────────────────────────────────────────────
    # Doc comments are expected where they explain a decision, not on every
    # public member, so `public_member_api_docs` stays off deliberately.
    public_member_api_docs: false
